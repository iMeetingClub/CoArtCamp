# 报名数据 → 飞书多维表格（复用南塘云村现有接口）

报名页提交链路：`enroll.html` → `/api/enrollments`（Cloudflare Pages Function）→ 飞书多维表格 API。

**不新建飞书应用**：直接复用南塘云村现有飞书应用与 Base（gnt 工具在用）。

**防重复**：提交时按「电话」列查重，已存在 → 409，页面提示「你已提交过报名」。**提交后锁定**（要改联系工作人员改表）。

## 1. 现有资源（勿新建）

| 项 | 值 | 位置 |
|----|----|------|
| App ID | `cli_a92572c37fb99ced` | `南塘DAO/gnt计算机制/工具/feishu_config.json` |
| App Secret | 同文件 `app_secret` | 同上 |
| App Token（Base） | 同文件 `app_token` | 同上 |
| 报名表 table_id | `tblqCQbAxP8yoLTF` | 已建好（表名「共创营报名」） |

**报名表已创建**：2026-09-01 用 `static-site/setup_enroll_table.py` 在现有 Base 中建好，22 个字段（提交ID/提交时间/来源 + 19 题列）。表在飞书里即可查看/筛选/导出 Excel。

## 2. 配置凭证（5 个环境变量）

- **本地调试**：`static-site/.dev.vars` 已生成（含真实凭证，被 .gitignore 忽略）。跑 `npx wrangler pages dev static-site`
- **线上部署**：Cloudflare Pages 项目 → Settings → Environment variables → 填 5 个变量：
  `FEISHU_APP_ID` / `FEISHU_APP_SECRET` / `FEISHU_APP_TOKEN` / `FEISHU_TABLE_ID` / `ADMIN_CODE`
  （`ADMIN_CODE` 是查看导出接口的邀请码，自己定一串随机值，不要告诉填报人）

## 3. 管理员查看/导出

- 日常看表：直接进飞书多维表格（表格视图、筛选、导出 Excel 都有）——与南塘云村 gnt 数据在同一个 Base，按表区分
- 接口导出：`GET https://站点/api/enrollments?code=你的ADMIN_CODE` → 全部记录 JSON

## 4. 若日后要换表/重建

1. 改字段：直接在飞书表格里增删列即可（函数按列名写入，列名与 19 题对应）
2. 换表：把新表 table_id 填进 env 的 `FEISHU_TABLE_ID`
3. 重建脚本：`python static-site/setup_enroll_table.py`（幂等：表已存在则补缺字段）

## 隐私注意

「健康状况」「电话」「姓名」为敏感字段：表权限只给管理员；填报人看不到表（表单入口只有网页报名页）。
