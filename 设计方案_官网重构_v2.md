# CoArtCamp 官网重构完整方案 v2

> 2026-08-27 · 已过 4 视角评审（视觉/UX/信息架构/工程）· 待用户拍板剩余决策点
> 红线（不变）：**数字准确最高优先级**（发现出入先停手问用户）· 不加新叙事内容 · 不改定稿叙事 · 不推送

---

## 〇、v2 相对 v1 的修订（评审采纳清单）

| # | 修订 | 来源 |
|---|------|------|
| 1 | **官网配色保持现状**（浅绿体系），不做换色迁移——用户拍板 | 用户 + 视觉评审发现 |
| 2 | 新增「官网→云村色调过渡」设计（报名流程渐变隐喻） | 用户拍板 |
| 3 | 报名弹窗完整交互规格（IME 守卫/键盘视口/焦点管理/pushState/三态提交/草稿/无障碍） | UX 评审 |
| 4 | 提交链路改走 **Cloudflare Pages Function 代理**（纯前端直连飞书必挂） | 工程评审 |
| 5 | 故事书减重：作品每期 3-6 幅精选 + 折叠；叙事并入时间线；archive 目录化；**phase 页保留为作品廊**（不做跳转壳） | UX + IA 评审 |
| 6 | iMeeting **15 条定稿纪事零改写**（v1 误写 10 条 + 日期错配，已纠正） | IA 评审 + 主会话核对 |
| 7 | 数字常量表（单一来源）+ nt-flow 修订冻结前流通小节只放定性文案 | IA 评审 |
| 8 | 修正云村 CORS 事实（FRONTEND_ORIGIN 需加官网域）；快照机制「脚本+手动跑+数字门禁」，禁 CI 自动 commit | 工程评审 |
| 9 | 弹窗实现路径（enroll.js 事件委托 + enroll.css 自包含令牌 + 每页 2 行 + 导航 modal 标记） | 工程评审 |
| 10 | phase 跳转用 `_redirects` 301（含存量规则改造），SPA 锚点异步滚动修复 | 工程评审 |

---

## 一、全站视觉规范

### 1.1 官网配色：保持现状（拍板）

官网继续使用现有浅绿体系（site.css `:root`：`--bg #edf4ed`、绿系 `--green-900/700/600/500 #55644e~#6d7d64`、蓝灰 `#6f8ea0~#7e9ba9`、muted `#708186`）。**全站页面/弹窗/组件必须使用这套，禁止引入募捐海报色板或云村色板。**

### 1.2 令牌收敛（不换色，收敛零散值）

- 圆角 3 档：卡片 24 / 媒体 20 / pill 999（收敛现 22-30px 八档）
- type scale：10/12/13/14/16/18/20/24/34/38；行高正文 1.7、标题 1.45
- spacing：4 的倍数；动效 280ms ease-out（页面/弹层）、160ms（微交互）、印章 500ms overshoot 只播一次
- 状态色：错误/成功/focus/禁用四态明确归属；focus 统一 2px outline + offset 3px
- 纸张纹理唯一来源：生产 bg-8.png，不新造点纹
- 数字排版：`font-variant-numeric: tabular-nums`（日期、进度、金额）
- z-index：drawer 39/40、lightbox 100、**弹窗 60+**

### 1.3 官网 → 云村 色调过渡（拍板）

报名弹窗按幕推进时，色调从官网浅绿**逐渐过渡**到云村深墨绿（云村 tokens.css：`--g-bg #1d2e24` / `--g-accent #3d6b52` / `--g-content #f2f5f2`）：
- 起点：ACT I = 官网色（浅绿底）
- 终点：完成页/进营地引导 = 云村色（深墨绿底）
- 过渡：按幕 5-6 档渐变（内容区保持浅色，背景与强调色渐变）——**具体档位待用户确认**
- 跳转云村后完全一致

### 1.4 印章使用边界

印章（朱砂类强调）= 郑重承诺/落款/完成，**全站任一视口最多一处**。允许：① 报名弹窗提交成功动画（唯一动作时机）；② 定稿叙事引文落款；③ 组办 credits 结尾。禁止：CTA 按钮、进度条、分节标记、错误提示、装饰平铺。规格：74-84px、2.5px 描边、rotate -8°~-10°、overshoot 500ms 只播一次、reduced-motion 时静态显示。

### 1.5 字体

标题 Noto Serif SC，正文 Noto Sans SC；fallback 宋体/黑体。手写体仅保留现有 hero handline 一处。

---

## 二、首页故事书

### 2.1 页面结构

```
banner（保留）
缘起 text-card（保留）
archive → 退化目录（期号+主题+一句，锚点跳转，删卡片正文与大图）
── 第一期 · 探路（#phase-1）
    钩子卡 → 事件节点时间线（含叙事） → 精选作品 3-4 幅 →「看全部 →」→ 流通与感谢摘要
── 第二期 · 验证（#phase-2）     同上
── 第三期 · 自然生长（#phase-3）  同上（另有共创人；议事节点让给 story 章，避免三重叙事）
── iMeeting 共创故事（#story）
    钩子卡 → 15 条定稿纪事（折叠展示，默认收起）→「完整纪事 →」
── 第四期报名行（#enroll）
    招募文案 + 「报名加入」→ 弹窗
组办 credits
共创与支持 + 捐助历史摘要（明细在丰碑页）
```

### 2.2 每期结构（去重后）

- **钩子卡** → 事件节点时间线（节点=日期+一句话事件+展开句，展开句即叙事本体，**不再另放叙事 text-card**）
- **作品**：每期精选 3-4 幅 +「展开全部 / 看这一期全部作品 →」链到 phase 页作品廊
- **流通与感谢摘要**：1-2 句感谢词 + 关键数字（nt-flow 修订冻结前只放定性文案，不放数字）
- phase 页**保留为作品廊**（不做跳转壳）；旧外链用 `_redirects` 301 到 `/#phase-N`

### 2.3 事件节点（逐期，已核对原文）

- 第一期（2025.11.15-30）：开营 22 人 → 集体会议定「传承工笔，共创南塘」→ 分组创作（工笔/水彩/拼贴）→ 结营画展「下次还要来」
- 第二期（2026.1.15-2.4）：开营（**部分**一期共创人成为工作人员——「部分」不可删）→ 主题「南塘人物志」→ 采访 20 位村民 → 线上线下同步画展
- 第三期（2026.6.6-22）：开营「寻找」26 人 → 分组三方向（自己/自然/关系）→ 营期延长动议（6.18）→ 城市快闪展（6.20 决议，6.24 举办）——议事规则学习等过程细节归 story 章
- iMeeting：**直接复用现有 15 条 timeline 数据，日期零改写**（4.16→6.23；含 6.18 掀开屋顶大会、6.20 快闪展决议、6.21 网站动议彩蛋）

### 2.4 导航与锚点

- sticky 期数 pill 导航（一/二/三/iMeeting/报名），滚过 banner 后出现
- 每期末「下一期 →」+「回到目录」
- SPA 锚点：renderPage 后读 location.hash 再 scrollIntoView（修复异步渲染锚点不滚）

### 2.5 数字常量表（单一来源）

site-data.js 顶部建数字常量表（每数字一行 + 注释溯源出处），nt-flow 页/丰碑页/故事书共同引用。**口径待用户定**：61,216.3 vs 61,216；119,763.6 vs 119,764；23 幅 vs 22 件人物志。

---

## 三、报名弹窗（不设独立 enroll.html 页面）

### 3.1 形态

- 全站报名按钮（导航 02、主页报名行、各处 CTA）→ 打开全屏弹窗（`role="dialog" aria-modal`）
- 移动端：100dvh 全屏 sheet（fallback 100vh），输入区靠上、按钮固定底部、visualViewport 监听保证焦点可见；桌面端：居中卡片 max-width 430、圆角 24、遮罩 rgba(28,45,38,.45)+blur
- 打开时 pushState `#enroll`（Android 返回键关弹窗而非离开网站；URL 可分享，点开直达弹窗）；popstate 关弹窗；关闭后历史还原
- 打开前先关抽屉（层管理：Esc/返回永远作用于最上层）
- 遮罩点击不直接关闭（防误触），仅关闭按钮

### 3.2 一问一答流程（≤12 屏）

五幕保留，字段合并：技能芯片一屏、健康两题合一屏、「为何相遇」与「最后想说的」合一屏。**必填/可跳过明确标注**：

1. Q1 你叫什么名字？（必填，零门槛开场）
2. Q2 怎么找到你？（**必填，前置到第 2 问**——单字段收微信/手机/邮箱，防中途流失丢线索）
3. Q3 来自哪里？
4. 技能芯片多选（一屏）
5. 为什么想加入？（必填）
6. 愿意承担什么？（可跳过）
7. 饮食禁忌 / 身体状况（可跳过；页内注明「仅用于营期照顾，开营后不保留」）
8. 最后想说的话（可跳过）
9. 回顾页：逐条「问题—答案」，**可点击跳回对应步骤改错**
10. 完成页：「呈上」→ 提交成功 → 印章动画 + 感谢语

- **IME 守卫**：keydown 里 `if (e.isComposing || e.keyCode === 229) return;`（中文输入法 Enter 不推进）；textarea 内 Enter 换行不推进
- 进度条：2px 描边轨道（line 色）+ pine 填充 + 文字计数「3 / 19」tabular-nums（不用 gold 作功能色）
- 幕标节奏：新幕首屏 = 金线渐变分隔 + 「ACT I · 认识你」；同幕后续屏只显示「ACT I · 3/6」；幕间 0.4s 金线展开过渡
- 每切一题 `aria-live` 播报新问题；芯片用 `button aria-pressed`（选中加对勾，不靠颜色单信号）；触控目标 ≥44px、芯片间距 ≥8px
- 焦点管理：打开聚焦第一输入框、Tab 困在弹窗内、关闭焦点还给触发按钮

### 3.3 视觉（A 式交互 + 官网色 + 克制装饰）

- 画布：paper 底 + bg-8.png 纸纹；内容边距 20px
- 排版：大标题 serif 24px/1.5/600 ink；引导语 sans 13px/1.7 muted（对比度 ≥4.5:1，加深或 14px）；题与输入间距 26px
- 输入：下划线式 1.5px line、focus 变 pine、字号 17px；错误提示 12px vermilion 置输入下方 + aria-live；单行 maxlength 50、多行 1000
- 脚部：主按钮 pine pill 999（padding 13px 34px、高 ≥44px），上一步 muted 文字按钮；禁用态 opacity .4
- **色调过渡（1.3）作用于此**：随幕推进背景/强调色从官网浅绿渐变到云村深墨绿
- 完成页：印章 84px overshoot 500ms 只播一次 → 感谢语 serif 20px + 13px muted
- 颜色纪律：vermilion 仅印章一处；gold 仅幕标分隔线；功能色一律 pine
- 表单 label：视觉隐藏 label 或 aria-label；联系方式 `inputmode="tel"`；正确 autocomplete 值

### 3.4 草稿与提交状态

- 草稿键 `coartcamp-enroll-draft-v1`（`{v:1, fields, savedAt}`）；写入 try/catch（Safari 隐私模式会抛）；每步推进 + 失焦存
- **健康信息不进草稿**（个保法敏感信息最小化）
- 提交三态：提交中（锁按钮）/ 成功（清草稿）/ **失败-未知送达**（保留草稿 + 明示「可能已收到，请勿盲目重试」+ **一键复制报名内容** + 引导发微信）
- 幂等：uuid 进 body + `Client-Token` 请求头（飞书官方支持，3h 内同值只触发一次）

---

## 四、提交链路（Pages Function 代理，不用纯前端直连）

**纯静态页直连飞书 webhook 必然失败**（CORS 预检必挂、token 泄露、1 req/s 限流、送达不可知——工程评审实测结论）。改为：

1. `static-site/functions/submit.js`（Cloudflare Pages 自动部署函数目录，wrangler.jsonc 已就位）：同域 POST → 服务端白名单校验字段 → 转发飞书 webhook（URL/token 在 env var，不进前端）
2. 服务端：字段白名单 + 长度校验 + 简单限频 + 重试退避；飞书 `Client-Token` 幂等
3. **上线必需项**（非占位）：备用联系方式（微信二维码）+ 失败态「复制报名内容」
4. 验收：上线前实弹提交一次，验证自动化落表（webhook 触发成功 ≠ 行已写入——异步自动化会静默失败；自动化里加「原文 JSON」兜底字段）
5. 这仍满足「官网零后端」= 无独立服务器、无新仓库、无 CI

---

## 五、两站互通（修正后）

| 层 | 内容 | 前置条件 |
|----|------|---------|
| ① 链接层 | 导航互链（钱包页→云村登录等） | 云村登录链接（待确认） |
| ② 数据快照 | 官网拉云村 /public/dashboard → 本地 JSON | 脚本进 repo + 手动跑 + **数字门禁**：跑完 diff，有变化停手问用户（红线行为）；禁 CI 自动 commit（可选 GitHub Actions cron → 只开 PR 不 merge） |
| ③ 实时只读 | 官网 fetch 云村 /public API | **需先修 CORS**：云村 backend `.env` 的 `FRONTEND_ORIGIN` 加 `https://coartcamp.imeeting.club`（+ `coartcamp.pages.dev` 预览域），`fly secrets set` + 重部署，main.py 零改动 |
| ④ 报名→进营 | 飞书收表 → 规范字段 → 云村导入 | 第四期；字段名以云村 C-9 数据字典为准（未确认前不写死） |

- 快照数据带溯源字段（week_key / root_hash / fetched_at）供 verify 验签；浏览器内重算 Merkle root 的验签功能**延后**（不绑重构一期）
- 云村侧：新只读端点（如 /public/camp/{phase}）须在法典 C-8 登记；报名字段在 C-9 登记「共创营报名」域
- 原则：官网永不反向依赖云村在线（快照兜底）；云村不依赖官网

---

## 六、募捐页 / 感谢页 / 领取返还（用户提出，细节待补）

- **募捐页**（非契约/纯捐赠）：第三期实绩 → 结余 5,209 元转投 → 第四期目标 NT 30,000 + RMB 20,000 → 不承诺返还 → 三种收款（占位）
- **感谢页（合作丰碑）**：位置待用户拍板（见待确认 1）
- **领取相关的返回原因**：规则说明——内容待用户详细说明后补入

---

## 七、实施清单（文件级）

1. **site-data.js**：数字常量表；home.sections 重构（archive 目录化、story-phase 章节、15 条 timeline、enroll-line、gratitude-note、donation-history 摘要）；navItems enroll 项加 `modal:true`
2. **app.js**：renderSection 加 case（story-phase/gratitude-note/enroll-line/donation-history + renderTimelineItem 提共享 + default 改 console.warn + 渲染可见占位）；renderCtaCard 加弹窗 button 分支；renderDrawer 加 modal 分支；renderPage 后锚点 scrollIntoView
3. **nav.js**：链接构造加 modal 分支（`<button data-enroll-open>` 而非 `<a>`）
4. **enroll.js 新建**：document 级事件委托 `[data-enroll-open]` → 弹窗（lightbox 成熟模式：body class + aria-hidden + Esc + focus trap）；导出 window.openEnroll()
5. **enroll.css 新建**：自包含令牌（以 site.css 实测值为准，pen 页不吃 site.css）
6. **每页 +2 行**：index / gratitude-design / nt-flow-progression（及未来 donate/wallet）加 `<link enroll.css>` + `<script type="module" src="scripts/enroll.js">`
7. **_redirects**：`/phase-1.html /#phase-1 301`（2/3/story 同）；存量 `_redirects` 规则改造（防双重跳转）；`/enroll.html → /#enroll 302`（老分享链接）
8. **functions/submit.js 新建**：webhook 代理（env：FEISHU_WEBHOOK_URL）
9. **gratitude-design.html**：`lang="en"` 改 `zh-CN`
10. **donate.html / wallet.html**：本版本建页或从导航隐藏——待用户定（现 nav 已 3 死链）

---

## 八、待确认事项（用户拍板后进 v3 施工）

1. **丰碑位置**（用户 Q1/Q3 回答矛盾）：撤不撤独立丰碑页？（Q1 选「全部嵌进首页」vs Q3 选「保留深页」）
2. **色调过渡形式**：按幕 5-6 档渐变（推荐）vs 完成页一次性过渡
3. **数字口径**：61,216.3 vs 61,216；119,763.6 vs 119,764；23 幅 vs 22 件
4. **phase 页保留为作品廊**（推翻原「全部跳转壳」决策——评审减重建议）——同意否？
5. **「报名相关问题」照片**：用户曾发（本会话未收到），需重发核对 19 字段
6. **「凡人能生」句义**：待用户澄清
7. **募捐/感谢/领取返还详细说明**：用户将另行提供
8. **飞书 webhook URL**（进 env）；群聊二维码；云村登录链接
9. **donate/wallet**：建页（按原计划）vs 导航隐藏
