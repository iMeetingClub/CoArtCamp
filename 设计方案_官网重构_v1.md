# CoArtCamp 官网重构完整方案 v1

> 2026-08-27 · 待多 AI 评审 · 评审通过后进入施工
> 红线（不变）：**数字准确最高优先级**（发现出入先停手问用户）· 不加新叙事内容 · 不改定稿叙事（「账在人心里——任务随机发放」等）· 不推送

---

## 一、目标

1. 官网（iMeetingClub/CoArtCamp，Cloudflare Pages 静态站）整体重构为「故事书」式一页叙事站
2. 全站**统一一个视觉风格**，延续现有官网气质（工笔 / 纸感 / 南塘绿），杜绝风格割裂
3. 报名形态：**不是独立页面，而是全站报名按钮点击后弹出的报名弹窗**（一问一答式交互）
4. 每期展示：**以事件节点叙事为主线**（发生的事），作品展示是其中一环，不是全部
5. 与南塘云村（项目/实景游戏）建立数据互通规范，为第四期「报名→进营」铺路

## 二、全站统一视觉规范（官网风格唯一来源）

### 2.1 官网配色：保持现状，不做换色迁移（用户拍板 2026-08-27）

官网继续使用现有浅绿体系（site.css：`--bg #edf4ed` / 绿系 #55644e~#6d7d64 / 蓝灰 #6f8ea0~#7e9ba9 / muted #708186），**所有页面/弹窗/组件必须遵守，禁止引入募捐海报色板或云村色板**。

### 2.2 令牌收敛（不换色，但收敛零散值）

现有 CSS 圆角 22-30px 八档、字号 20+ 个、间距各取所需——收敛为规范刻度（视觉评审建议）：
- 圆角 3 档：卡片 24 / 媒体 20 / pill 999
- type scale：10/12/13/14/16/18/20/24/34/38；行高正文 1.7、标题 1.45
- spacing：4 的倍数；动效 280ms ease-out（页面/弹层）、160ms（微交互）
- 状态色：错误/focus/禁用/成功四态明确归属；focus 统一 2px outline
- 纸张纹理唯一来源：生产 bg-8.png，不新造点纹
- 数字排版：`font-variant-numeric: tabular-nums`
- z-index 层级：drawer 39/40、lightbox 100、弹窗 60+（写死防打架）

### 2.3 官网 → 云村 色调过渡（用户拍板 2026-08-27）

**核心设计**：报名流程 = 「从官网走进云村」的色调渐变隐喻。报名弹窗按幕（ACT I-V）推进时，色调从官网浅绿体系**逐渐过渡**到云村深墨绿体系（云村 tokens.css：`--g-bg #1d2e24` / `--g-accent #3d6b52` / `--g-content #f2f5f2`）。

- 起点：ACT I = 官网色（浅绿 #edf4ed 底）
- 终点：完成页/进营地引导 = 云村色（深墨绿底 + 云村强调绿）
- 过渡方式（待定稿）：按幕递增的渐变阶（建议 5-6 档，内容区保持浅色、背景与强调色渐变；避免浅→深跳变突兀）
- 跳转云村后完全一致（无再过渡）

### 2.4 印章元素使用边界（视觉评审规则，采纳）

印章（朱砂类强调）= 郑重承诺/落款/完成，**全站任一视口最多一处**。允许时机：① 报名弹窗提交成功动画（唯一动作时机）；② 定稿叙事引文落款；③ 组办 credits 结尾。禁止：CTA 按钮、进度条、分节标记、错误提示、装饰平铺。规格：74-84px、2.5px 描边、rotate -8°~-10°、overshoot 500ms 只播一次。

### 2.5 字体

标题 Noto Serif SC（衬线），正文 Noto Sans SC；fallback 系统宋体/黑体。手写体（现有 hero handline）仅保留现有使用处，不扩展。

## 三、首页故事书（含每期事件节点展示）

### 3.1 页面结构（从上到下）

```
banner（保留：艺文共创 / 第四期招募）
缘起 text-card（保留）
历期索引 archive（保留，作目录）
── 第一期 · 探路 ──（#phase-1）
    钩子卡 → 事件节点时间线 → 叙事 → 作品 → 流通与感谢
── 第二期 · 验证 ──（#phase-2）    同上结构
── 第三期 · 自然生长 ──（#phase-3）  同上结构（另有共创人）
── iMeeting 共创故事 ──（#story）
    钩子卡 → 事件节点时间线（10 条纪事）
── 第四期报名行 ──（#enroll）
    招募文案 + 「报名加入」按钮 → 点击弹出报名弹窗
组办 credits
共创与支持 + 捐助历史段
```

### 3.2 每期「事件节点」设计（新增，区别于纯作品墙）

每期以 4-6 个事件节点组成叙事主线，节点 = 日期 + 一句话事件 + 一句展开（来源：各期 text-card 现有叙述，不新写）：

- **第一期 · 探路**（2025.11.15-30，15 天）
  1. 开营：22 位共创人从全国各地而来
  2. 集体会议：没有预设主题，大家一起定「传承工笔，共创南塘」
  3. 分组创作：工笔画 / 水彩 / 拼贴，回应南塘的日常
  4. 结营画展：在南塘宿舍举办，很多人说「下次还要来」
- **第二期 · 验证**（2026.1.15-2.4，22 天）
  1. 开营：一期共创人正式成为工作人员，「经验被继承」
  2. 主题「南塘人物志」：工笔画为主线
  3. 采访 20 位村民：记录南塘的平民史诗
  4. 线上线下同步画展：23 幅人物志
- **第三期 · 自然生长**（2026.6.6-22，17 天）
  1. 开营：主题「寻找」，26 人参与
  2. 议事规则学习：先学会怎么一起做决定（南塘十二条）
  3. 自荐队长 + 自主选组：三个方向——寻找自己 / 自然 / 关系
  4. 营期延长动议：大家不太想走
  5. 城市快闪展：从共创营内部长了出来
- **iMeeting 共创故事**（2026.4.16-6.23）
  4.16 议题提出「第三期要不要做」→ 5.18 目标辩论 → 5.21 与合作社定边界 → 6.5 动议通过（主题「寻找」+ 599 元）→ 6.7 规则学习 → 6.8 分组 → 6.11 展览落地细节 → 6.18 掀开屋顶大会 → 6.21 营期延长 → 6.23 快闪展

作品展示（works）在节点之后，作为「这些事件长出的果实」。照片灯箱交互保持。

### 3.3 渲染实现

- site-data.js：新增 `story-phase` 类型（id/期名/钩子/events[]/children[]），events 渲染为时间线节点；`gratitude-note` 流通小节；`enroll-line` 报名行；`donation-history` 捐助段
- app.js：对应渲染器 + 锚点输出 + timeline 换行修复
- phase-1/2/3.html、story.html 改跳转壳（→ index.html#锚点）

## 四、报名弹窗（不是独立页）

### 4.1 形态

- 全站报名按钮（主页报名行、导航「02 报名加入」、募捐页转化 CTA、各处入口）点击 → **弹出全屏报名弹窗**（430px 容器内，遮罩 + 居中卡片，滚动锁定）
- 不设独立 enroll.html 页面；弹窗组件随官网 SPA/各页共享

### 4.2 交互：一问一答（已确认采用 demo A）

- 一次一个问题，Enter / 点击推进，渐变过渡，顶部进度条 + 幕标（ACT I-V）
- 五幕结构（19 字段模板，待用户照片核对）：
  - ACT I 认识你（姓名 / 来自 / 联系方式…）
  - ACT II 光谱技能（芯片多选）
  - ACT III 为何相遇（为什么想加入）
  - ACT IV 共同生活（愿意承担什么）
  - ACT V 照顾你（饮食禁忌 / 身体状况）
- 完成页：回顾 + 「呈上」按钮 → 印章动画「已呈上」→ 感谢语
- localStorage 自动存草稿（关弹窗重进不丢）

### 4.3 视觉：A 式交互 + 官网统一风格

- 底色 paper 米白 + 纸纹，标题 Noto Serif SC，主按钮 pine 绿 pill
- 装饰（克制）：幕标题前金线分隔、进度条 gold、完成页 vermilion 印章、顶部小字「南塘共创营 · 第四期」
- 排版规范：统一间距节奏（28px 边距）、每步大标题 24px + 引导语 13px 灰、输入框下划线式（focus 变 pine）

## 五、提交与存储（官网零后端原则不变）

1. 提交 → POST 飞书多维表格 webhook（URL 待用户提供；未配置时前端提示 + 备用联系方式）
2. 字段规范：19 字段命名与云村数据字典兼容（`name / city / contact / skills[] / reason / duties / diet / health…`），带时间戳 + 来源（coartcamp-web）
3. 第四期开营：导出 → 导入云村 → 共创人飞书登录云村 → 营地档案
4. 不需要第二套存储

## 六、两站互通（官网 ⇄ 云村）

| 层 | 内容 | 状态 |
|----|------|------|
| ① 链接层 | 导航互链（钱包页→云村登录等） | 施工期做 |
| ② 数据快照 | 官网定期拉云村 /public 接口 → 本地 JSON | 中期 |
| ③ 实时只读 | 官网 fetch 云村 /public API（CORS 已放行）+ 数字可链 verify.html 验签 | 中期 |
| ④ 报名→进营 | 飞书收表 → 规范字段 → 云村导入 | 第四期 |

- 云村侧规范：新只读端点（如 /public/camp/{phase}）须在法典 C-8 接口契约登记；报名字段在 C-9 数据字典登记「共创营报名」域
- 原则：官网永不反向依赖云村在线（快照兜底）；云村不依赖官网

## 六·5、NT 数据流通与云村结合（用户提出，方案要点）

现状问题：官网 nt-flow-progression.html 的流通数字是**手写死数据**（一期 61,216.3 / 二期 119,763.6 / 三期 24,293…），且用户已知流水与余额对不上（云村侧待修）。结合方案：

1. **数据同源**：官网 NT 流通页数字不再手写——改为拉取云村 `/public/dashboard`（NT 流通汇总）生成**数据快照**（本地 JSON，定期刷新）；快照刷新用部署 CI 或手动脚本，官网保持纯静态
2. **数字可验证**：每个展示数字带「验签入口」→ 链到云村 `verify.html` 独立验证器（哈希链密封 + 周锚定），数字信任从「我们核对」升级为「可独立验证」
3. **锚定展示**：NT 流通页加「最新周锚定」块——云村 anchor_service 每周期打点 0xdead 烧毁地址、带 explorer_url，直接展示区块链存证
4. **流水对齐**：云村侧先修正流水/余额数据（用户另行处理），快照从权威数据来，官网自动对齐——官网不再承担「自己核对」的责任
5. **同源扩展**：募捐页（目标/结余/返还记录）与感谢页（丰碑名单/金额）未来同样从云村账本快照取数，与 NT 流通同源
6. **实时可选**：中期 CORS 加入 CoArtCamp 域名后，可加浏览器端实时 fetch（快照兜底）

## 六·6、募捐页 / 感谢页 / 领取返还（用户提出，细节待补）

用户已点名这三部分「需要注意的部分也要加进去」，具体如何添加**将另行详细说明**：

- **募捐页**（非契约 / 纯捐赠性质）：第三期实绩 → 结余 5,209 元转投 → 第四期目标 NT 30,000 + RMB 20,000 → 不承诺返还 → 三种收款（占位）
- **感谢页（合作丰碑）**：手风琴形态已定（一期/二期/三期/八月四块碑面），数字溯源需求
- **领取相关的返回原因**：领取（NT/凭证）与返还的原因与规则说明——内容待用户详细说明后补入

> 待补细节（用户会再发说明）：以上三部分的具体呈现方式与文案要点

## 七、待确认事项

1. 「报名相关问题」清单——用户曾发（照片/视频），本会话未收到，需重发核对 19 字段
2. 飞书 webhook 地址
3. 群聊二维码、收款码（募捐页占位）
4. 南塘云村登录链接（钱包页外链）
5. archive 卡片保留（当前方案：保留作目录）
6. 「凡人能生」句义未解（用户原话：「逐渐把这个归置于凡人能生的这个其他的」）——待用户澄清
7. 募捐页/感谢页/领取返还的详细说明（用户将另行提供）
